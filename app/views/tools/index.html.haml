.row
  .columns.large-12
    = render "breadcrumbs"
    - title "Tools"

    %ul.large-block-grid-5.small-block-grid-2
      - 10.times do
        = render @tools

    - if current_or_null_user.can_create?(Tool)
      = link_to "New Tool", new_tool_path
