%fieldset
  %legend Links
  %p.hint It can include blogs, social networks, video/photo sites etc.
  = f.simple_fields_for :links do |link|
    = render 'link_fields', f: link
  .links
    = link_to_add_association f, :links, class: 'button tiny' do
      = fa_icon "plus"
      Add another link


