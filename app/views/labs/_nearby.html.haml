- if nearby_labs
  .heading= t('views.labs.show.nearby_labs')
  %ul.nearby-labs
    - nearby_labs.each do |lab|
      %li.nearby-lab
        = link_to lab, class: 'pull-left' do
          = image_tag hocho(lab.avatar_url, "o=t&q=80&d=90x90&"), class: 'small avatar'

        .media-body
          .media-heading= link_to lab, lab, class: 'nearby-lab-name'
          .distance= "#{lab.distance.ceil}km away"
