- title "Labs"

%table
  %thead
    %tr
      %td ID
      %td Name
      %td Creator
      %td Slug
      %td State
      %td Created at

  - @labs.each do |lab|
    = content_tag_for :tr, lab do
      %td= lab.id
      %td= link_to lab, lab
      %td= link_to lab.creator, lab.creator
      %td= lab.slug
      %td= lab.current_state
      %td= l(lab.created_at, format: :short)
