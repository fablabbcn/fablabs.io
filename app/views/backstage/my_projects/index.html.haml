= render "backstage/header"

.container
  .row
    .col-lg-12
      = title "My Projects"

      = render 'backstage/my_projects/projects', q: @q, url_form: [:backstage, :my_projects], projects: @projects
