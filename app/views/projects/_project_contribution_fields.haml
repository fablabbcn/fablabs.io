.row
  .columns.large-6
    %label
      Contributors
    .contributors-container
      .icon
        %span
          = icon('terminal')
    .contributors
      - @project.contributions.each do |c|
        - if c.contributor.present?
          = image_tag hocho(c.contributor.avatar, "o=t&q=80&d=90x90"), class: 'avatar tiny'

  .columns.large-6
    %label
      Search and select
    = select_tag :contributor_id
