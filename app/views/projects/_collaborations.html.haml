%h5 Labs
%ul
  - if @project.lab.present?
    %li
      = image_tag hocho(@project.lab.avatar, "o=t&q=80&d=90x90"), class: 'avatar tiny'
      = link_to @project.lab do
        %strong= @project.lab.name
  - @project.collaborators.each do |collaborator|
    %li
      = image_tag hocho(collaborator.avatar, "o=t&q=80&d=90x90"), class: 'avatar tiny'
      = link_to collaborator do
        %strong= collaborator.name
