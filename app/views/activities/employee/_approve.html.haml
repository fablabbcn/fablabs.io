- if employee.present?
  .vertical-timeline-block
    .vertical-timeline-circle
    .vertical-timeline-content
      = content_tag_for :div, activity do
        .text-large
          = link_to employee.lab, employee.lab
        %h5
          = link_to activity.actor, activity.actor, class: 'user'
          was added as #{employee.job_title} at
          = employee.lab
        .text-small
          %i #{time_ago_in_words(activity.created_at)} ago
