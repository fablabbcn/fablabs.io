.row
  .col-lg-12
    = title "API"
    %p Welcome to the API. It is currently READ-ONLY. Although this will change very soon once we have an initial spec.

    %p
      You can see a JSON list of labs here - #{link_to api_v0_labs_url(format: 'json'), api_v0_labs_url(format: 'json')}
